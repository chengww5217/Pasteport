{
  "name": "pasteport",
  "displayName": "Pasteport",
  "description": "Paste images and files from your local clipboard into a remote terminal — the remote path lands in the prompt, ready for a CLI agent to read.",
  "version": "0.0.1",
  "publisher": "chengww5217",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chengww5217/pasteport.git"
  },
  "bugs": {
    "url": "https://github.com/chengww5217/pasteport/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "remote",
    "clipboard",
    "paste",
    "terminal",
    "ssh"
  ],
  "extensionKind": [
    "ui"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pasteport.paste",
        "title": "Paste into Remote Terminal",
        "category": "Pasteport"
      },
      {
        "command": "pasteport.cancelTransfer",
        "title": "Cancel Transfer",
        "category": "Pasteport"
      },
      {
        "command": "pasteport.cleanUpRemoteFiles",
        "title": "Clean Up Remote Files",
        "category": "Pasteport"
      },
      {
        "command": "pasteport.diagnose",
        "title": "Diagnose",
        "category": "Pasteport"
      }
    ],
    "keybindings": [
      {
        "command": "pasteport.paste",
        "key": "ctrl+shift+v",
        "win": "ctrl+v",
        "mac": "cmd+v",
        "when": "terminalFocus"
      }
    ],
    "configuration": {
      "title": "Pasteport",
      "properties": {
        "pasteport.remoteDir": {
          "type": "string",
          "default": "/tmp/pasteport",
          "markdownDescription": "Absolute POSIX directory on the remote host where pasted files land. `~` is not expanded."
        },
        "pasteport.quoting": {
          "type": "string",
          "enum": [
            "auto",
            "shell",
            "none"
          ],
          "enumDescriptions": [
            "Currently equivalent to none; will adapt once TUI quoting behaviour is verified.",
            "Single-quote paths that contain special characters, for use in a shell.",
            "Insert paths verbatim, for TUI agents that do not parse shell syntax."
          ],
          "default": "auto",
          "description": "How remote paths are quoted when inserted into the terminal."
        },
        "pasteport.trailingSpace": {
          "type": "boolean",
          "default": true,
          "description": "Append a space after the inserted path so you can keep typing."
        },
        "pasteport.confirmAboveSeconds": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "markdownDescription": "Ask before transfers estimated to take longer than this many seconds, using the measured transfer rate. Set to `0` to never ask."
        },
        "pasteport.ttlHours": {
          "type": "number",
          "default": 24,
          "minimum": 0,
          "markdownDescription": "Age at which pasted files are removed from the remote host and the local staging directory. Set to `0` to disable cleanup."
        },
        "pasteport.bracketedPaste": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Wrap the inserted path in bracketed paste markers (`ESC[200~` … `ESC[201~`). Off by default; compatibility across TUIs is unverified."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -p ./ -w",
    "lint": "eslint src --ext ts",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "npm run compile && node --test dist/test/*.test.js",
    "package": "npm run compile && npx --yes @vscode/vsce package -o pasteport.vsix",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "20.11.30",
    "@types/vscode": "1.85.0",
    "@typescript-eslint/eslint-plugin": "7.18.0",
    "@typescript-eslint/parser": "7.18.0",
    "eslint": "8.57.1",
    "prettier": "3.3.3",
    "typescript": "5.5.4"
  }
}
